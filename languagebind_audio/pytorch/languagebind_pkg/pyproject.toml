[project]
name = "languagebind"
version = "0.0.1"
description = "LanguageBind: Extending Video-Language Pretraining to N-modality"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["."]
