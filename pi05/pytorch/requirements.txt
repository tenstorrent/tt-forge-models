# Pi-0.5 (lerobot) runtime dependencies.
#
# IMPORTANT: lerobot itself is installed via requirements.nodeps.txt with
# --no-deps. Installing it with its full dependency set pulls torchaudio from
# the default PyPI index, which is the CUDA build and force-replaces the
# torch-stack '+cpu' wheels the torch-xla device path needs. So torch /
# torchvision / torchaudio are deliberately NOT listed here and must not be
# touched by this file.
numpy<2.3.0,>=2.0.0
Pillow<13.0.0,>=10.0.0
einops<0.9.0,>=0.8.0
draccus==0.10.0
gymnasium<2.0.0,>=1.1.1
safetensors<1.0.0,>=0.4.3
termcolor<4.0.0,>=2.4.0
tqdm<5.0.0,>=4.66.0
transformers<5.6.0,>=5.4.0
scipy<2.0.0,>=1.14.0
