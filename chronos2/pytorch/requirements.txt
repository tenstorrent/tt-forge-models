# chronos-forecasting requires transformers<5 which conflicts with the global
# transformers>=5 environment. Install it without deps via requirements.nodeps.txt
# to avoid downgrading transformers and breaking torch.compile guard evaluation.
